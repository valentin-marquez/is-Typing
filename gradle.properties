# Done to increase the memory available to Gradle.
# Optimized settings for low memory usage (2 clients + 1 server)
org.gradle.jvmargs=-Xmx768M -Xms256M -XX:+UseG1GC -XX:MaxMetaspaceSize=256m -XX:+UseStringDeduplication
org.gradle.parallel=false
org.gradle.caching=true
org.gradle.daemon=true
org.gradle.configureondemand=false

# Reduce worker count for better stability
org.gradle.workers.max=1

# Mod properties
mod_version = 1.0.0
maven_group = com.nozz.it
archives_name = istyping
enabled_platforms = fabric

# Minecraft properties
minecraft_version = 1.20.4
yarn_mappings = 1.20.4+build.1

# Dependencies
architectury_api_version = 11.1.17
fabric_loader_version = 0.17.2
fabric_api_version = 0.97.3+1.20.4